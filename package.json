{
  "name": "vamsasetu",
  "version": "1.0.0",
  "description": "Intelligent family relationship visualization and event system",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && ./mvnw spring-boot:run",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && ./mvnw clean package",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && ./mvnw clean install"
  },
  "keywords": ["family-tree", "relationships", "events", "notifications"],
  "author": "VamsaSetu Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
